const BASIC_USER = 'basic';
const PREMIUM_USER = 'premium';

const QUOTA_EXCEEDED_MESSAGE = 'You have exceeded your monthly quota, Upgrade to premium';
const MOVIE_ADAPTOR_ERROR = 'Could not get movie details at the moment, Please try again';
const MOVIE_CREATION_ERROR = 'An error occurred while trying to create movie';
const INVALID_ARGUMENT = 'Invalid Arguments Provided';
const INVALID_CREDENTIALS = 'Invalid Credentials';

const REQUEST_BODY = 'body';
const REQUEST_HEADER = 'headers';
const REQUEST_QUERY = 'query';
const REQUEST_PARAM = 'params';

module.exports = {
  BASIC_USER,
  PREMIUM_USER,
  QUOTA_EXCEEDED_MESSAGE,
  MOVIE_ADAPTOR_ERROR,
  MOVIE_CREATION_ERROR,
  INVALID_ARGUMENT,
  INVALID_CREDENTIALS,
  REQUEST_BODY,
  REQUEST_HEADER,
  REQUEST_QUERY,
  REQUEST_PARAM,
};
